import React from 'react';
import Layout from './shared/Layout';

const SearchProjects = (props) => {
  return (
    <Layout>
      <h1>SearchProjects</h1>
    </Layout>
  );
}

export default SearchProjects;
